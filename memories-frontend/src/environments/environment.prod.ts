export const environment = {
  production: true,
  // 👉 After creating the Render web service, replace this with its URL + /api,
  // e.g. 'https://memories-backend.onrender.com/api'
  apiBaseUrl: 'https://YOUR-RENDER-SERVICE.onrender.com/api',
};
